#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>
#include <string.h>
#include "png-tools.h"

int png_signature(FILE *f)
{
  fprintf(stderr,"TODO: png_signature\n");
  exit(1);
}

uint32_t read_int(FILE *f)
{
  fprintf(stderr,"TODO: read_int\n");
  exit(1);
}

char *read_chunk_tag(FILE *f)
{
  fprintf(stderr,"TODO: read_chunk_tag\n");
  exit(1);
}

struct dims *png_dimensions(FILE *f)
{
  fprintf(stderr,"TODO: png_dimensions\n");
  exit(1);
}

chunk_list *png_map(FILE *f)
{
  fprintf(stderr,"TODO: png_map\n");
  exit(1);
}

char *extract_tEXt(FILE *f, uint32_t i)
{
  fprintf(stderr,"TODO: extract_tEXt\n");
  exit(1);
}
